export class Constellation {
    constructor(
        public id: string,
        public name: string,
        public date?: string,
        public path?: string,
        public description?: string
    ) { }
}
